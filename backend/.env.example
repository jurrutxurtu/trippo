# Copy to .env. Never commit the real file.
MAPTILER_KEY=

# A Google AI Pro subscription does NOT cover the API -- that is billed separately.
# Check a key with:  python -m trippo.cli ai-check
GEMINI_API_KEY=
# Optional. Default is gemini-flash-latest, an alias so a retired model cannot break it.
GEMINI_MODEL=

# Paid, off by default. See docs/decisions/ADR-0010.
GOOGLE_PLACES_API_KEY=
