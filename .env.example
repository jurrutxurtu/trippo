# Domain for automatic HTTPS with Caddy (e.g. yourname.duckdns.org or leave empty for localhost)
DOMAIN=yourname.duckdns.org

# Optional MapTiler API Key (falls back to OSM raster tiles if empty)
MAPTILER_KEY=

# Optional Gemini API Key for light suggestions
GEMINI_API_KEY=
